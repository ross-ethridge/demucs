# pgdata — Postgres data, single-pod
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pgdata
  namespace: demucs
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: local-path
