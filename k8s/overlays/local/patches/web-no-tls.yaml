apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
  namespace: demucs
spec:
  template:
    spec:
      containers:
        - name: web
          env:
            - name: TLS_DOMAIN
              value: ""
